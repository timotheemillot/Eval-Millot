package com.example.eval_millot

import kotlin.test.Test
import kotlin.test.assertEquals

// Garde le jeu de sources de tests partagé branché sur la compilation.
class ComposeAppCommonTest {

    @Test
// Confirme que la tâche de tests communs exécute bien des assertions.
    fun example() {
        assertEquals(3, 1 + 2)
    }
}
